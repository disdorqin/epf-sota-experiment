# Work Log: 2026-07-01

## SOTA 单模型实验区创建

在 `models/` 目录下创建了独立实验工程，用于评估 CatBoost 替换 LightGBM、Chronos 替换 TimesFM。

### 完成的工作

1. **扫描原仓库**：确认原仓库路径 `electricity_forecast_model2.0`，核心文件在 `fusion/` 命名空间下
2. **创建目录结构**：configs/、src/common/、src/models/、src/experiments/、src/reports/、scripts/、outputs/、tests/
3. **实现 common 层**：
   - `data_loader.py` — 中文列名兼容的数据加载
   - `business_time.py` — 1秒偏移法业务时间映射（hour 24 → 下一自然日 00:00）
   - `feature_builder.py` — 21维特征工程（复现原 LightGBM）
   - `metrics.py` — sMAPE_floor50（严格匹配原仓库实现）
   - `output_schema.py` — long table 输出格式
   - `split_utils.py` — 时间序训练/测试切分
4. **实现 CatBoost 适配器**：`catboost_adapter.py` + `run_catboost_single_day.py`（已验证输出24行）
5. **实现 Chronos 适配器**：`chronos_adapter.py` + `run_chronos_single_day.py`（含自动 fallback）
6. **实现 walk-forward**：`run_sota_walkforward.py`（支持多模型/多任务/多日期）
7. **实现对比报告**：`build_report.py` + `compare_sota_vs_original.py`
8. **编写测试**：14个单元测试全部通过

### CatBoost 验证结果

- 成功训练（500轮迭代，35K训练样本）
- 成功预测 2026-02-01（日前电价）— 24行
- hour_business 范围 1-24，hour 24 对应 2026-02-02 00:00 ✅
- 使用 direct fit/predict（避免 Pool 对象在 Windows 上的 segfault）

### 已知约束

- Chronos 适配器代码已完成，但环境无 torch/chronos 包，未运行验证
- 脚本使用 `os.path.abspath(__file__)` 确保在含中文路径下稳定运行
- 推荐用 `python.exe "D:/.../scripts/run_*.py"` 绝对路径方式运行

## 2026-07-02 补充

### Chronos 验证结果
- 修复了 `chronos-forecasting` 包的 `__init__.py` 缺失问题（pip 安装不完整）
- Chronos-2 (`amazon/chronos-2-small`) 在 HuggingFace 不可用（需 gated access），自动 fallback 到 Chronos-Bolt
- Chronos-Bolt-Small (44M params) 成功加载并推理
- 成功预测 2026-02-01（日前电价）— 24 行输出 ✅
- hour_business 1-24，hour 24 = 下一自然日 00:00 ✅
- 使用 quantile-based 输出 API（9个分位数），p50 作为 y_pred
- 已知：Chronos-2 在华东镜像站不可用，需配置 HF_TOKEN 尝试访问

### GitHub 仓库
- 创建私有仓库：**epf-sota-experiment** → `https://github.com/disdorqin/epf-sota-experiment`
- 已推送主分支 main，32 个源文件，无遗留文件
- 环境：conda epf-2（Python 3.11.14），已安装 catboost + chronos-forecasting + torch + transformers
